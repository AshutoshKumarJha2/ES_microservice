spring:
  application:
    name: vendor-manager
  profiles:
    active: local
  config:
    import: "configserver:http://localhost:6971"

springdoc:
  swagger-ui:
    disable-swagger-default-url: true
    path: /api/v1/vendor-manager
#
#logging:
#  config: classpath:logback-spring.xml
#  file:
#    path: ./logs
#  level:
#    root: INFO
#    com.cts.eventsphere.vendormanager: DEBUG
#    org.hibernate.SQL: DEBUG

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6969/eureka