spring:
  application:
    name: expense-manager

  config:
    import:
      configserver:http://localhost:6971

  profiles:
    active: local

server:
  port: 7073

eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:6969/eureka

services:
  event:
   url: http://localhost:7072
  auth:
   url: http://localhost:7070

service:
  name: expense-manager
  secret: es-svc-expense-manager-2026

