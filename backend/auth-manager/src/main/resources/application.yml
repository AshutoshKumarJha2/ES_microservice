spring:
  application:
    name: auth-manager
  config:
    import: configserver:http://localhost:6971
  profiles:
    active: local
  cloud:
    config:
      profile: default

server:
  port: 7069

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6969/eureka