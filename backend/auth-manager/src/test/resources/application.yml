spring:
  application:
    name: auth-manager
  config:
    import: ""
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false

server:
  port: 7070

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
